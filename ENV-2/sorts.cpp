#include "sorts.h"
#include <algorithm>

// it wasn't said if I need to write the sorting algorithm myself, soooo...

void sortComplex(std::vector<ComplexNumber>& obj){
    std::sort(obj.begin(), obj.end());
}

